id: "derby_civicweb"

# Base host and entry (root folder) URL for Derby's CivicWeb library
base_url: "https://derby.civicweb.net"
root_folder_url: "/filepro/documents/4440/"

# City name for output directory structure
city_name: "Derby"

# Target year to scrape
target_year: 2025

# Allowed domains for basic validation
allowed_domains:
  - "derby.civicweb.net"

# Output directory relative to backend/
output_dir: "data/raw notes"

